package logging.pattern;

public class PatternUtil {

  // Uses pattern to convert raw message into log message
  public static String resolveMessageusingPattern(String message, String pattern) {
    return message;
  }
}
